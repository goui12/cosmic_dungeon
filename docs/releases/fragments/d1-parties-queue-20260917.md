# D1 party invitations and leader queue

The shared class-selector Ready pool could combine unrelated players and start as
soon as its configured size was reached. Tamsin now owns personal invitations,
leader-led 3-6-player rosters, individual ready confirmations and explicit queue submission.

Membership, class and eligibility changes clear readiness. Pending onboarding invites
do not reserve seats. Groups enter FIFO after an instance becomes available, and lock
when preparation begins. Q&A D24's no-merge rule is preserved. Completing one run no
longer clears another waiting lobby at the same selector. Entry revalidates the roster
and checks safe landing positions after startup schematics are pasted.

New timing keys live under TamsinVane in CosmicDungeon.config. Existing MaxPlayers
remains the selector's capacity ceiling. No new save IDs, registry changes or vendor prices.
Pre-entry lobbies clear on restart; established agreements and active run saves persist.
Protocol 3 requires matching client/server jars. No new PNG or datagen output is required.

Java 21 build and offline party/packet/config regression checks are recorded in the
batch 03 audit. Licensed multiplayer, GUI, dedicated-server and failure-injection
acceptance remain pending. No deployment or game runtime was performed.
