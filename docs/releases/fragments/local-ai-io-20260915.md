# Local AI and TEST I/O onboarding

Added local-only encrypted SFTP setup, pinned host verification, guarded deployment, build receipts, bounded log/process capture, and cache/decision guidance. No gameplay, registry, networking, persistence, runtime dependency, or client resource requirement changes.
