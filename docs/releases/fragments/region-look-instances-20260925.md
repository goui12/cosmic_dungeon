# Region outlines in dungeon instances

Named and nearby region visualization now uses the existing template mapping to display authored boundaries in the player's current dungeon instance. Other worlds and instances remain separate. Region protection rules and saved bounds are unchanged.
